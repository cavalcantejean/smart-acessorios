
// This file has been emptied as the comment moderation system,
// which relied on Server Actions, is not compatible with
// `output: 'export'` in Next.js.
// If comment moderation is needed in a static site,
// it would typically be handled by a backend service (e.g., Cloud Function)
// called from the client, or by a third-party moderation service.
export {}; // Add an empty export to make it a module
